#!/usr/bin/env sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required to commit in this repo. Install it with: brew install gitleaks" >&2
  exit 1
fi

gitleaks protect --staged --redact
