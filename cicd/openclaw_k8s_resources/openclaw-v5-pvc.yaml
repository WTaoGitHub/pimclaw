apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ PVC_NAME }}
  namespace: {{ NAMESPACE }}
  labels:
    app: {{ DEPLOYMENT_NAME }}
    dynamic: 'true'
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: nexdata-block