apiVersion: v1
kind: Service
metadata:
  name: {{ SERVICE_NAME }}
  namespace: {{ NAMESPACE }}
  labels:
    app: {{ DEPLOYMENT_NAME }}
spec:
  selector:
    app: {{ DEPLOYMENT_NAME }}
  ports:
    - name: http
      port: 18789
      targetPort: http